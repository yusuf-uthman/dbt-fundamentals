
version: 2

models:
  - name: stg_customers
    description: "A starter dbt model"
    columns:
      - name: "ID"
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: stg_orders
    description: "A starter dbt model"
    columns:
      - name: "ID"
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
